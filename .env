# TaskWeaver Environment Configuration
# ====================================

# Server Configuration
PORT=3000
NODE_ENV=development

# Email Configuration (Gmail)
EMAIL_USER=millionaftermillion@gmail.com
EMAIL_PASS=vmql xube zece tdpt

# Security
SESSION_SECRET=taskweaver_super_secret_key_2024_secure_

# Database Configuration
# Replace <user>, <password>, <host>, <port>, <dbname> with the values from Render
DATABASE_URL=postgres://<user>:<password>@<host>:<port>/<dbname>

# Application Settings
APP_NAME=TaskWeaver
APP_VERSION=2.0.0
APP_URL=http://localhost:3000

# Reminder Settings (in minutes)
DEFAULT_REMINDER_INTERVAL=20

# Deadline Reminder (in days)
DEADLINE_REMINDER_DAYS=1


# Optional: For production deployment
# DATABASE_URL=sqlite:/data/database.sqlite
# SESSION_SECRET=your_very_strong_secret_key_here
